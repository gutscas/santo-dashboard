name: CI/CD Pipeline

on:
  push:
    branches: [ main ]

jobs:
  # ====================================
  # 1️⃣ TEST & BUILD JOB (CI)
  # ====================================
  test-and-build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      # --- Backend Setup & Tests ---
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install Backend Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run Django Tests
        run: python manage.py test

      # --- Frontend Setup & Build ---
      - name: Set up Node.js via NVM
        run: |
          # Install NVM
          curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.6/install.sh | bash
          export NVM_DIR="$HOME/.nvm"
          [ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"

          # Install & use Node 20.19 (Vite requirement)
          nvm install 20.19
          nvm use 20.19
          nvm alias default 20.19

      - name: Install Frontend Dependencies
        working-directory: ./santo-project
        run: npm ci

      - name: Build Frontend
        working-directory: ./santo-project
        run: npm run build
        env:
          VITE_API_BASE_URL: /dashboard-api  # ✅ Added this

  # ====================================
  # 2️⃣ DEPLOY JOB (CD)
  # ====================================
  deploy:
    needs: test-and-build
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'

    steps:
      - name: Deploy to Server via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SERVER_IP }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            # --- Go to project folder ---
            cd ~/santo-dashboard

            # --- Pull latest code ---
            git pull origin main

            # --- Backend setup ---
            source venv/bin/activate
            pip install --upgrade pip
            pip install -r requirements.txt

            # --- Node.js setup (NVM) ---
            export NVM_DIR="$HOME/.nvm"
            [ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"
            nvm use 20.19 || nvm install 20.19

            # --- Frontend build with environment variable ---
            cd santo-project
            npm install
            export VITE_API_BASE_URL=/dashboard-api  # ✅ Added this
            npm run build
            cd ..

            # --- Django migrations & collectstatic ---
            python manage.py migrate
            python manage.py collectstatic --noinput

            # --- Restart services ---
            sudo systemctl daemon-reload
            sudo systemctl restart gunicorn-santo
            sudo systemctl restart nginx